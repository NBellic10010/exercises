package main

import "fmt"

func main() {
	/**
	li := Generate_List()
	var p *GlistNode
	var x int32
	var inf t
	inf.value = "Tran"
	li.add(inf, "DATA", nil)
	p = li.head
	x = li.getoveralllength(0, p)
	//y := li.length
	fmt.Print(x)

	const xn="231321"
	s := new(S)
	s.set(10)
	x := s.get()
	fmt.Print(x)

	*/
	//fmt.Print(reflect.TypeOf('('))
	ct := longestValidParentheses_2("()(()")
	fmt.Print(ct)

}
